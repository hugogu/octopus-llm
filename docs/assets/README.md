# docs/assets

Place the homepage hero screenshot here as **`hero.png`** (referenced by the top-level `README.md`).

Recommended capture: the chat comparison view with several models answering one prompt, each
generated HTML block toggled to **Run** so the live result renders. Crop to just the model panels
(exclude the sidebar and empty space) for a clean, not-too-tall image.
